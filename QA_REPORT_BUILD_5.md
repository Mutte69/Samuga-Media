# Build 5 website QA

- `node --check` passed for every JavaScript file.
- Every HTML file was parsed and checked for duplicate IDs.
- All local HTML script, stylesheet and image references were checked.
- CSS brace-balance checks passed for `styles.css` and `admin.css`.
- The dashboard contains the Authors view, author editing dialog, user-to-author link controls and Super Admin article deletion controls.
- No font file is included; Faruma is requested as an external webfont with MV Faseyha and Noto Sans Thaana fallbacks.
